'use server'

// Server Actions du pôle marketing-social — pas de saisie manuelle (décision Benoît, cf.
// SocialTemplate.tsx) : Instagram/Telegram sont collectés automatiquement, Twitter attend
// son automate. `saveSocialEntries`/`addSocialAccount` (+ leur dialog, social-entry-dialog.tsx)
// ont été retirés (audit simplify wave 1, ~2026-07) — aucun appelant restant.
export {}
