{
  "name": "@glagency/ingestion",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "dev": "tsx watch src/main.ts",
    "capture": "tsx src/capture.ts",
    "cf:dev": "wrangler dev --test-scheduled --port 8799",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@glagency/core": "workspace:*",
    "@glagency/db": "workspace:*",
    "@glagency/mypuls": "workspace:*",
    "@sentry/cloudflare": "^10.63.0"
  },
  "devDependencies": {
    "@types/node": "^22",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "wrangler": "^3.90.0"
  }
}
