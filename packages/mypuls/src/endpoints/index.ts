// Endpoints MyPuls (chacun = un fetch typé + parser pur).
// TODO: stats (dashboard/stats), subscriptions (dashboard/subscriptions),
// messagingMoney (scrape HTML creator/messaging-money-team via cheerio),
// creators + users (API v1 /creators, /users role MESSAGING → affectations).
export {}
