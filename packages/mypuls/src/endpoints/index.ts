// Endpoints MyPuls (chacun = un fetch typé + parser pur).
// Faits : team-money (API), money-team (scrape HTML), dashboard (stats + subscriptions).
// TODO: creators + users (API v1 /creators, /users role MESSAGING → affectations).
export {}
