# Carte — glagencyapp

À lire **avant** d'explorer le code : une ligne par feature. Feature ajoutée, renommée ou supprimée → sa ligne dans le même commit, sinon `release:prepare` refuse. Les descriptions ne sont pas contrôlées : une ligne fausse repérée se corrige dans le commit qui la découvre.

## Web — apps/web

| Feature | À quoi elle sert | Entrée | Dossier | Données | Doc |
| --- | --- | --- | --- | --- | --- |
| bilan | Bilan hebdomadaire par modèle (CA, KPIs, une carte par modèle) | `/chatter/bilan` (`BilanTemplate`) | `apps/web/src/features/bilan/` | tables `creator_daily`, `creators` ; rpc `bilan_report` | — |
| chatters | Table de performance des chatteurs sur la période (CA, actifs) | `/chatter/chatters` (`ChattersTemplate`) | `apps/web/src/features/chatters/` | rpc `chatters_report` (via `@/lib/services/get-chatters`, hors dossier feature) | — |
| compta | Paie des chatteurs par période de 14 jours (saisie, primes, dettes, paiement) ; aussi montée sous Marketing pour la paie staff | `/chatter/compta`, `/marketing/compta` (`ComptaTemplate`) | `apps/web/src/features/compta/` | tables `chatter_creator_daily`, `chatters`, `compta_day_entries`, `compta_debts`, `compta_payments`, `compta_primes`, `compta_rates`, `compta_setter_scale`, `compta_settings`, `compta_week_entries`, `creators`, `police_entries`, `profiles` ; rpc `chatter_first_seen` | `docs/superpowers/specs/2026-07-27-compta-paie-design.md` |
| health | Tracker d'objectif LTV : jauge agence + plan de rattrapage par modèle | `/chatter/health` (`HealthTemplate`) | `apps/web/src/features/health/` | tables `chatters`, `creators` ; rpc `health_report` | — |
| impersonation | Bandeau « consulter/agir en tant que » — pas de route dédiée, tissé dans le layout dash | `(dash)/layout.tsx` (`ImpersonationBanner`) | `apps/web/src/features/impersonation/` | table `profiles` | `docs/superpowers/specs/2026-07-23-impersonation-consulter-en-tant-que-design.md` |
| infos-modeles | Fiche modèle par accordéon (identité, listes, récits, texte) | `/chatter/infos-modeles` (`InfosModelesTemplate`) | `apps/web/src/features/infos-modeles/` | table `creators` | — |
| insights | Cartes Analyses hebdo (quotas) par chatteur, splittées par modèle | `/chatter/insights` (`InsightsTemplate`) | `apps/web/src/features/insights/` | tables `chatter_daily`, `chatters`, `creators`, `insight_states`, `insights`, `profiles` ; rpc `mypuls_presence_by_chatter` | `docs/superpowers/specs/2026-07-03-glagency-insights-design.md` · `ARCHITECTURE.md` § Domaines — Équipes et quotas |
| marketing-dashboard | Vue d'ensemble marketing (performance des liens) | `/marketing/overview` (`DashboardTemplate`) | `apps/web/src/features/marketing-dashboard/` | table `mkt_link_daily` | — |
| marketing-liens | Classement des liens de tracking + graphique d'évolution journalière | `/marketing/liens`, `/marketing/liens/groupes` (`LiensTemplate`) | `apps/web/src/features/marketing-liens/` | tables `mkt_link_daily`, `mkt_link_groups`, `mkt_links` | `docs/superpowers/specs/2026-09-22-mkt-groupes-liens-design.md` · `ARCHITECTURE.md` § Domaines — Groupes de liens marketing |
| marketing-modeles | Ce que les liens de tracking rapportent à chaque modèle (CA, abonnés) | `/marketing/modeles` (`ModelesTemplate`) | `apps/web/src/features/marketing-modeles/` | tables `mkt_link_daily`, `mkt_source_notes` ; rpc `mkt_creator_revenue` | `docs/superpowers/specs/2026-09-08-marketing-modeles-design.md` · `ARCHITECTURE.md` § Domaines — Groupes de liens marketing |
| marketing-social | Comptes sociaux par plateforme (Instagram, Telegram, Twitter) | `/marketing/instagram`, `/marketing/telegram`, `/marketing/twitter` (`SocialTemplate`) | `apps/web/src/features/marketing-social/` | tables `creators`, `mkt_social_accounts`, `mkt_social_daily`, `mkt_staff` | — |
| marketing-staff | Fiches VA (rémunération, assignations aux liens ; la paie se règle dans Compta) | `/marketing/staff` (`VaTemplate`) | `apps/web/src/features/marketing-staff/` | tables `mkt_link_daily`, `mkt_links`, `mkt_social_accounts`, `mkt_social_daily`, `mkt_staff`, `mkt_staff_links`, `mkt_staff_payments` ; rpc `mkt_save_staff_assignments` | — |
| members | Gestion des membres (comptes, droits, modèles assignés) + onglet Turnover | `/chatter/members`, `/formation/members`, `/marketing/members` (`MembersTemplate`) | `apps/web/src/features/members/` | tables `chatters`, `compta_primes`, `compta_rates`, `compta_settings`, `creators`, `member_events`, `profile_creators`, `profiles`, `training_legacy_claim_attempts`, `training_legacy_claims` ; rpc `training_legacy_login_holder`, `turnover_report` | `docs/superpowers/specs/2026-07-03-glagency-members-design.md` |
| models | Comparatif modèles (CA total, part privée) | `/chatter/modeles` (`ModelsTemplate`) | `apps/web/src/features/models/` | tables `chatter_creators`, `chatters`, `creators` ; rpc `models_report` | — |
| mypuls-chatter-activity | Fiche activité minute par minute d'un chatteur sur une journée (source MyPuls) | `/chatter/presence/:profileId` (`MypulsChatterActivityTemplate`) | `apps/web/src/features/mypuls-chatter-activity/` | tables `mypuls_shift_settings`, `profiles` ; rpc `mypuls_shift_chatter` | `ARCHITECTURE.md` § Domaines — Relevé MyPuls (Présence) |
| mypuls-shift-report | Relevé d'équipe de présence, source MyPuls — seule mesure de présence réelle (le tracker Electron porté n'a jamais été repointé, ses tables sont vides) | `/chatter/presence` (`MypulsShiftReportTemplate`) | `apps/web/src/features/mypuls-shift-report/` | tables `chatters`, `mypuls_shift_settings`, `profiles` ; rpc `mypuls_shift_board`, `mypuls_shift_board_range` | `docs/superpowers/specs/2026-09-01-releve-mypuls-design.md` · `ARCHITECTURE.md` § Domaines — Relevé MyPuls (Présence) |
| mypuls-shift-settings | Réglages du relevé MyPuls (créneaux, journal des runs, rattachements orphelins) | `/chatter/presence/reglages` (`MypulsShiftSettingsTemplate`) | `apps/web/src/features/mypuls-shift-settings/` | tables `mypuls_shift_settings`, `profiles` ; rpc `mypuls_shift_settings_page` | `ARCHITECTURE.md` § Domaines — Relevé MyPuls (Présence) |
| organisation | Board d'organigramme de l'agence (cellules, placement par équipe) | `/chatter/organisation` (`OrganisationTemplate`) | `apps/web/src/features/organisation/` | tables `creators`, `profile_creators`, `profiles` ; rpc `move_org_team`, `save_org_cell`, `save_org_row`, `set_org_placement_kind` | `docs/superpowers/specs/2026-08-17-exclusion-organisation-design.md` |
| overview | Vue d'ensemble CA (global + MyPuls + Uncove) par modèle | `/chatter/overview` (`OverviewTemplate`) | `apps/web/src/features/overview/` | tables `chatter_creators`, `creators`, `profiles` ; rpc `overview_ca_global`, `overview_report` | — |
| planning | Planning journalier des membres, dépliable, filtre `?membre=` | `/chatter/planning` (`PlanningTemplate`) | `apps/web/src/features/planning/` | tables `planning_blocks`, `plannings`, `profiles` | — |
| police | Tracker de sanctions chatteurs (saisie + journal, période du header) | `/chatter/police` (`PoliceTemplate`) | `apps/web/src/features/police/` | tables `chatter_creators`, `police_entries`, `profile_creators`, `profiles` | `docs/superpowers/specs/2026-07-07-glagency-police-tracker-design.md` |
| police-reports | Rapport du soir, section Police | `/chatter/rapport-police` (`PoliceReportsTemplate`) | `apps/web/src/features/police-reports/` | tables `creators`, `police_reports`, `profile_creators`, `profiles` ; rpc `upsert_police_report` | `docs/superpowers/specs/2026-07-21-rapport-police-design.md` · `ARCHITECTURE.md` § Domaines — Rapport du soir police |
| quotas | Seuils journaliers par équipe + exclusions LTV (alimente les cartes Analyses) | `/chatter/quotas` (`QuotasTemplate`) | `apps/web/src/features/quotas/` | tables `creators`, `quotas`, `teams` | `ARCHITECTURE.md` § Domaines — Équipes et quotas |
| recruit-admin | Back-office recrutement (candidats, config du test, intégrations) | `/formation/recrutement`, `/formation/recrutement/config` (`RecruitTemplate`) | `apps/web/src/features/recruit-admin/` | tables `profile_creators`, `profiles`, `recruit_blocklist`, `recruit_candidates`, `recruit_config`, `recruit_messages` | — |
| recruit-test | Test de recrutement public, machine à états (5 tentatives/IP/24h, pas de barème affiché) | `/postuler` (`TestFlow`) | `apps/web/src/features/recruit-test/` | tables `recruit_attempts`, `recruit_blocklist`, `recruit_candidates`, `recruit_config`, `recruit_messages` ; rpc `recruit_start_attempt` | `docs/superpowers/specs/2026-08-20-formation-recrutement-design.md` |
| reports | Comptes rendus journaliers (« Dashboard »), rédaction du sien, lecture hiérarchique | `/chatter/dashboard` (`ReportsTemplate`) | `apps/web/src/features/reports/` | tables `daily_reports`, `profiles` | `docs/superpowers/specs/2026-07-16-daily-reports-design.md` |
| repos | Planning repos hebdo, grille éditable par colonnes/cellules | `/chatter/repos` (`ReposTemplate`) | `apps/web/src/features/repos/` | tables `chatters`, `creators`, `profiles`, `rest_planning_cells`, `rest_planning_column_members`, `rest_planning_weeks` ; rpc `copy_repos_week`, `repos_data_weeks`, `save_repos_cell` | `docs/superpowers/specs/2026-07-07-glagency-repos-colonnes-ids-design.md` |
| scripts | Funnel de messages par modèle (scripts éditables par l'admin) | `/chatter/scripts` (`ScriptsTemplate`) | `apps/web/src/features/scripts/` | tables `creators`, `script_items` | — |
| snap-codes | Identifiants Snapchat par modèle, édition inline autosave | `/chatter/codes-snap` (`SnapCodesTemplate`) | `apps/web/src/features/snap-codes/` | tables `creators`, `snap_codes` | `ARCHITECTURE.md` § Domaines — Codes Snap |
| sources-trafic | Lecture des sources de trafic par modèle (écriture dans marketing-modeles) | `/chatter/sources-trafic` (`SourcesTraficTemplate`) | `apps/web/src/features/sources-trafic/` | tables `creators`, `mkt_source_notes` ; rpc `mkt_model_sources` | `ARCHITECTURE.md` § Domaines — Groupes de liens marketing |
| spenders | Suivi des gros spenders (liste, tracker quotidien, alertes, archive) | `/chatter/spenders/{liste,tracker,alertes,archive}` (`SpendersTemplate`) | `apps/web/src/features/spenders/` | tables `creators`, `relances`, `spender_conversations`, `spender_crm` ; rpc `crm_spenders_kpis_json`, `crm_spenders_page`, `crm_spenders_tracker_json` | — |
| stat-chatteur | KPIs de closing + classement setters/closers | `/chatter/stat-chatteur` (`StatChatteurTemplate`) | `apps/web/src/features/stat-chatteur/` | table `profiles` (closing lu via `@/lib/services/closing-by-chatter`, hors dossier feature) | `docs/superpowers/specs/2026-07-22-stat-chatteur-classement-design.md` |
| stats | Courbes d'abonnés par modèle | `/chatter/stats` (`StatsTemplate`) | `apps/web/src/features/stats/` | tables `creator_daily`, `creators` | — |
| todos | To-do personnelle (onglet de Planning), chacun gère la sienne | `/chatter/planning?vue=todo` (`TodosTemplate`) | `apps/web/src/features/todos/` | tables `profiles`, `todos` | `docs/superpowers/specs/2026-07-20-todos-kanban-design.md` · `ARCHITECTURE.md` § Domaines — To-do personnelle |
| tracking-board | Board de shift porté de l'ancien tracker Electron (`/d/:shift/:date`) — **orphelin** : aucune route ne l'importe, `tracker_window` n'a jamais été repointé, remplacé par mypuls-shift-report | aucune | `apps/web/src/features/tracking-board/` | rpc `tracker_window` (via `readTrackerWindow`) ; table `profile_creators` | — |
| tracking-chatter | Fiche chatteur du tracker porté (périodes) — **orphelin**, même cause que tracking-board | aucune | `apps/web/src/features/tracking-chatter/` | rpc `tracker_window` (via `readTrackerWindow`) | — |
| tracking-coaching | Suivi chatters, port de `/notes` (notes, skills, todo coaching) | `/chatter/presence/suivi`, `/chatter/presence/suivi/:profileId` (`CoachingTemplate`) | `apps/web/src/features/tracking-coaching/` | tables `profile_creators`, `profiles`, `tracker_chatter_notes`, `tracker_ratings`, `tracker_sessions`, `tracker_skills`, `tracker_todo_tasks` ; rpc `tracker_coaching_list` | `ARCHITECTURE.md` § Domaines — Suivi chatters |
| tracking-managers | Présence des encadrants, port de `/m/:date` | `/chatter/presence/managers` (`ManagersTemplate`) | `apps/web/src/features/tracking-managers/` | rpc `tracker_window` (via `readTrackerWindow`) | — |
| tracking-recap | Récap hebdomadaire de mission, port de `/recap` | `/chatter/presence/recap` (`RecapTemplate`) | `apps/web/src/features/tracking-recap/` | table `profile_creators` ; rpc `tracker_todo_week_recap` | `ARCHITECTURE.md` § Domaines — To-Do du tracker |
| tracking-todo | To-Do hebdomadaire d'équipe, port de `/todo` (grille, habitudes) | `/chatter/presence/todo` (`TodoTemplate`) | `apps/web/src/features/tracking-todo/` | tables `profiles`, `tracker_chatter_notes`, `tracker_ratings`, `tracker_sessions`, `tracker_skills`, `tracker_todo_daily`, `tracker_todo_dayoff`, `tracker_todo_habits`, `tracker_todo_links`, `tracker_todo_notes`, `tracker_todo_sections`, `tracker_todo_tasks` | `ARCHITECTURE.md` § Domaines — To-Do du tracker |
| training-ai | Analytics IA de la Formation (admin) : coût, usage, par cas/chatteur | `/formation/ia` (`AiTemplate`) | `apps/web/src/features/training-ai/` | rpc `training_ai_by_case`, `training_ai_by_chatter`, `training_ai_cost`, `training_ai_daily` | `docs/ia-formation-couts.md` |
| training-catalog | Catalogue admin des modules/cours/cas d'entraînement | `/formation/catalogue` (`CatalogTemplate`) | `apps/web/src/features/training-catalog/` | tables `training_boss_fan_secrets`, `training_case_arena_slots`, `training_case_boss_fans`, `training_case_messages`, `training_case_secrets`, `training_cases`, `training_module_axes`, `training_module_secrets`, `training_module_sections`, `training_modules` | `docs/superpowers/specs/2026-08-17-formation-catalogue-design.md` |
| training-legacy | Encart de reprise « ancienne plateforme » — pas de route propre, intégré dans `/formation/ma-formation` | `/formation/ma-formation` (`LegacyClaimCard`) | `apps/web/src/features/training-legacy/` | table `training_legacy_claims` | `docs/superpowers/specs/2026-08-24-formation-reprise-gla-design.md` |
| training-me | « Ma formation » : hero, modules, podium, trophées, classement | `/formation/ma-formation` (`MeTemplate`) | `apps/web/src/features/training-me/` | tables `training_case_bests`, `training_profile_stats`, `training_sessions` ; rpc `training_ranking`, `training_weekly_ranking` | — |
| training-module-wheel | « Ma roue » : roue de récompenses côté chatter | `/formation/ma-roue` (`ModuleWheelTemplate`) | `apps/web/src/features/training-module-wheel/` | tables `training_module_wheel_config`, `training_wheel_spins`, `training_wheel_tickets` ; rpc `training_module_wheel_state` | `docs/superpowers/specs/2026-08-30-roue-modules-chatter-design.md` |
| training-modules | Page d'un module : classement, cours, cas | `/formation/modules`, `/formation/modules/:code` (`ModuleTemplate`) | `apps/web/src/features/training-modules/` | rpc `training_module_ranking` | — |
| training-overview | Vue d'ensemble Formation (admin) : roster, coût IA, déblocages de tentatives | `/formation/overview` (`OverviewTemplate`) | `apps/web/src/features/training-overview/` | tables `profiles`, `training_attempt_grants`, `training_case_bests`, `training_cases`, `training_modules`, `training_reports`, `training_sessions` ; rpc `training_ai_cost`, `training_axis_profile`, `training_overview_roster` | `docs/superpowers/specs/2026-09-14-limite-essais-design.md` |
| training-session | Une session d'entraînement : jeu actif ou issue notée/ratée/abandonnée | `/formation/session/:id` (`SessionTemplate`) | `apps/web/src/features/training-session/` | tables `training_case_secrets`, `training_cases`, `training_messages`, `training_profile_stats`, `training_reports`, `training_sessions`, `training_threads` | — |
| training-wheel | Page Roue côté encadrant (tirage en partage d'écran, historique, config) | `/formation/roue` (`WheelTemplate`) | `apps/web/src/features/training-wheel/` | tables `profiles`, `training_wheel_config`, `training_wheel_spins`, `training_wheel_tickets` ; rpc `training_overview_roster` | — |
| uncove | Analytics Uncove par compte (Subs + CA), lecture seule | `/chatter/uncove/modeles` (config admin), `/chatter/uncove/stats` (`UncoveAccounts`, `UncoveDashboard`) | `apps/web/src/features/uncove/` | tables `creators`, `uncove_account_tokens`, `uncove_accounts`, `uncove_daily` | `docs/runbook-uncove.md` · `ARCHITECTURE.md` § Domaines — Uncove |

## Packages

| Package | À quoi il sert | Dossier | Données | Doc |
| --- | --- | --- | --- | --- |
| core | Logique métier partagée, pure (types domaine, règles d'assignation/impersonation, matching des groupes de liens marketing, moteur d'insights/quotas, résumé d'ingestion) — aucun accès DB, consommé par `apps/web` et `apps/ingestion` | `packages/core/` | aucune (pure logique) | — |
| db | Accès Supabase service-role (`createAdminClient`), types `Database` générés, chiffrement des tokens, `fetchAll` (pagination) — consommé par `apps/ingestion` (le web garde son client SSR propre) | `packages/db/` | aucune table propre (fournit l'accès) | — |
| mypuls | Client HTTP + parseurs pour la plateforme MyPuls (login, team money, dashboard, chat, scripts, shifts) — consommé par `apps/ingestion` | `packages/mypuls/` | aucune (scrape HTTP) | — |
| uncove | Client HTTP + parseurs pour la plateforme Uncove (wallet, abonnés, transactions) — consommé par `apps/ingestion` | `packages/uncove/` | aucune (scrape HTTP) | `docs/superpowers/specs/2026-09-21-uncove-analytics-design.md` |

## Ingestion — apps/ingestion

Hors contrôle (`check-carte.mjs` ne couvre que les racines Web et Packages ci-dessus). Worker Cloudflare (`src/worker.ts`) + CLI Node locales, mêmes briques réutilisables (`*-core.ts`).

| Commande / Schedule | À quoi elle sert | Fichier | Données | Doc |
| --- | --- | --- | --- | --- |
| `pnpm start` | Pipeline complet MyPuls chatteurs : login, ingestion CA/scripts, agrégats jour — manuel/local, même code que le cron `5 23` | `src/main.ts` → `pipeline.ts` | tables `chatter_alias`, `chatter_creator_daily`, `chatter_daily`, `chatters`, `creator_daily`, `creators` | — |
| `pnpm capture <url>` | Outil dev : capture une page MyPuls authentifiée en fichier brut, pour écrire un parser dans `@glagency/mypuls` | `src/capture.ts` | aucune | — |
| `pnpm check-session` | Vérifie que le cookie de session MyPuls injecté est encore valide, sans rien écrire | `src/check-session.ts` | aucune | — |
| `pnpm backfill-scripts` | Rejoue `creator_script_daily` sur des jours précis (rattrapage après un trou de session) | `src/backfill-scripts.ts` → `spenders-core.ts` | table `creator_script_daily` | — |
| `pnpm spenders` | Backfill/run manuel du scrape spenders (mêmes briques que le cron `0 0`) | `src/spenders.ts` → `spenders-core.ts` | tables `chatter_alias`, `chatters`, `creators`, `fan_transactions`, `spender_conversations` | — |
| `pnpm shifts` | Backfill/run manuel du contrôle des shifts MyPuls (mêmes briques que le cron `30 4`) | `src/shifts.ts` → `shifts-core.ts` | tables `chatter_alias`, `chatters`, `creators`, `mypuls_day_kpi`, `mypuls_shift_runs`, `mypuls_shift_settings`, `profiles` | — |
| `pnpm marketing` | Rattrapage des liens de tracking sur une plage arbitraire (le run nocturne ne réécrit qu'une fenêtre glissante de 8 jours) | `src/marketing-cli.ts` → `marketing.ts` | tables `creators`, `mkt_link_daily`, `mkt_link_groups`, `mkt_links` | — |
| `pnpm insights` | Génère les cartes Analyses hebdo sans passer par le Worker | `src/gen-insights.ts` → `insights.ts` | tables `chatter_creator_daily`, `chatter_daily`, `chatters`, `creators`, `insights`, `mypuls_shift_runs`, `quotas` ; rpc `mypuls_presence_by_chatter` | — |
| `pnpm uncove [jours]` | Relevé Uncove (Subs + CA), fenêtre glissante par défaut 35 j (mêmes briques que le cron `0 5`) | `src/uncove.ts` → `uncove-core.ts` | tables `uncove_account_tokens`, `uncove_accounts`, `uncove_daily` | `docs/superpowers/specs/2026-09-21-uncove-analytics-design.md` |
| cron `5 23 * * *` | Run principal : pipeline chatteurs + fan-out marketing (liens de tracking) — 3 crons actifs/5 (plafond Free par compte) | `src/worker.ts` → `pipeline.ts` | mêmes tables que `pnpm start` | — |
| cron `0 0 * * *` | Scrape spenders, fan-out 1 requête self par modèle (budget CPU/sous-requêtes Free) | `src/worker.ts` → `spenders-core.ts` | mêmes tables que `pnpm spenders` | — |
| cron `30 4 * * *` | Contrôle des shifts MyPuls, deux jours par run | `src/worker.ts` → `shifts-core.ts` | mêmes tables que `pnpm shifts` | `docs/superpowers/specs/2026-09-01-releve-mypuls-design.md` |
| cron `0 5 * * *` | Relevé Uncove (Subs + CA par compte actif) | `src/worker.ts` → `uncove-core.ts` | mêmes tables que `pnpm uncove` | `docs/superpowers/specs/2026-09-21-uncove-analytics-design.md` |
