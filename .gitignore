# deps
node_modules/
.pnpm-store/

# build
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# env / secrets
.env
.env.*
!.env.example

# supabase local
packages/db/supabase/.branches/
packages/db/supabase/.temp/

# test / coverage
coverage/

# Claude — tout ignoré sauf les skills (source de vérité archi, partagée avec Codex via .agents/skills)
.claude/*
!.claude/skills/

# misc
.DS_Store
*.log


# Ingestion : dumps bruts + logs (données business)
apps/ingestion/raw/
apps/ingestion/logs/
.dev.vars

# Artefacts Playwright — e2e/.auth/*.json contient des refresh tokens Supabase VALIDES
# (sessions de la préprod, qui est une copie des données de prod). Ne jamais committer.
apps/web/e2e/.auth/
apps/web/playwright-report/
apps/web/test-results/
.vercel
.env*

# Pages et CSS d’origine du tracker GLA — références de portage, données réelles, jamais versionnées.
.tracker-ref/
